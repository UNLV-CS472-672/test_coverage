# Dependencies for this lab.
# Flask/Werkzeug 2.2 relied on ast.Str, which was removed in Python 3.14,
# so these are pinned to the 3.x line. Requires Python 3.9 or later.
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
pytest>=8.0,<9.0
pytest-cov>=5.0,<8.0
Werkzeug>=3.0,<4.0
